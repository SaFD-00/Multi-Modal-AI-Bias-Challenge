HF_TOKEN=your_huggingface_token_here

# 학습 GPU 프로파일 (src.train.launch이 읽음). global batch는 항상 64로 유지된다.
GPU_TYPE=A100        # A100 | H100 | RTX5090
GPU_COUNT=1          # 1 | 2
GPU_DEVICES=0        # (선택) 물리 GPU 인덱스. 예 "0" 또는 "0,1". 미설정 시 0..GPU_COUNT-1
