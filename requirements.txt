# 데이터 구축 + 학습 + 추론 통합 의존성.
# CUDA 휠은 환경에 맞게 설치(예: pip install torch --index-url https://download.pytorch.org/whl/cu128).
# ⚠️ 학습용 transformers(>=4.49, 실사용 4.57.6 핀)와 추론용 vLLM은 버전 고정이 충돌할 수 있다.
#    한 환경에서 학습+추론을 모두 돌리면 vLLM이 transformers/torch를 자체 버전으로 끌어와 충돌 가능 →
#    추론(src.predict)은 별도 venv에서 `pip install vllm pydantic`로 설치 권장(평가환경: RTX A6000 48GB,
#    Python 3.10, CUDA 12.4, PyTorch 2.6.0).

# 공통
pandas
pillow
pyyaml
tqdm
python-dotenv

# 데이터 구축
datasets
huggingface_hub
pytest

# 학습 (A100/H100 80GB 또는 RTX5090)
torch
transformers>=4.49
peft
accelerate
wandb

# 추론/제출 (src.predict) — vLLM은 transformers/torch를 함께 고정(위 ⚠️ 참조)
vllm
pydantic
