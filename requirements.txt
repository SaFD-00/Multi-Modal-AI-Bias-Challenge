# CUDA 휠은 환경에 맞게 설치(예: pip install torch --index-url https://download.pytorch.org/whl/cu121).

# 데이터 구축용
datasets
huggingface_hub
pandas
pillow
pyyaml
pytest
tqdm
python-dotenv

# 학습 전용 (A100 80GB x 1)
torch
transformers>=4.49
peft
accelerate
wandb
